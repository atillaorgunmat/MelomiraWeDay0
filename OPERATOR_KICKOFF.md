# Operator Kickoff (fresh cycle)

**Where we are:** Bank/graph/trace reset to empty. Clarifiers exist in prior GUIDANCE logs; no nodes are frozen.
**Goal:** Select a small nucleus (1–2 items max capacity) and freeze them via `pro_request`.

## Route (copy/paste)
ROUTE → GUIDANCE (FREE) for P-FND-0001: question_nomination (fresh nucleus options; no numeric caps)

## What to paste under that route
- Any clarifier_round YAML you want considered.
- Any context links or recent notes.

## After GUIDANCE replies
1) ROUTE → SELECT-ORG (FREE) for P-FND-0001
   - Paste the entire `question_nomination:` YAML.
   - Ask for `selection_decision:` YAML only.
2) ROUTE → GUIDANCE (FORM) for P-FND-0001: pre‑PRO pro_request (may include q_patch)
3) ROUTE → AUTO (FORM) for P-FND-0001: task=AUTO‑VERIFY
4) ROUTE → AUTO (FORM) for P-FND-0001: task=AUTO‑APPLY (Operator will do the mechanical PR)
5) ROUTE → AUTO (FORM) for P-FND-0001: task=AUTO‑READ (...confirm inventory/gaps/trace)
